rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Public read for transparency website
    match /{document=**} {
      allow read: if true;
      // TODO: Replace with Firebase Auth once admin uses Firebase login
      allow write: if true;
    }
  }
}
